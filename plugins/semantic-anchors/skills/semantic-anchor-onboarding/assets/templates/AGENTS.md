# Semantic Anchors

Apply these semantic anchors by default at the start of every session unless the user overrides them.

## Active Anchors

- `{{PRIMARY_ANCHOR_1}}`: `{{PRIMARY_EXPLAINER_1}}`
- `{{PRIMARY_ANCHOR_2}}`: `{{PRIMARY_EXPLAINER_2}}`
- `{{PRIMARY_ANCHOR_3}}`: `{{PRIMARY_EXPLAINER_3}}`

## Working Rules

- Name the relevant anchor when it materially affects a recommendation, design, review, or code change.
- If two selected anchors pull in different directions, ask which one takes precedence before making a major change.
- Treat these anchors as defaults rather than laws; explicit user instructions win.
- Keep suggestions concrete and aligned with the selected anchors.
