# Semantic Anchors

Use these semantic anchors as persistent project memory unless the user overrides them.

## Active Anchors

- `{{PRIMARY_ANCHOR_1}}`: `{{PRIMARY_EXPLAINER_1}}`
- `{{PRIMARY_ANCHOR_2}}`: `{{PRIMARY_EXPLAINER_2}}`
- `{{PRIMARY_ANCHOR_3}}`: `{{PRIMARY_EXPLAINER_3}}`

## Working Rules

- Mention the active anchor when it changes the recommendation.
- Ask before mixing anchors from a known conflict group.
- Prefer the selected house vocabulary over synonyms.
- Treat direct user instructions as higher priority than this file.
