# Semantic Anchors

Use these semantic anchors as repository-wide defaults for coding, review, and planning unless the user overrides them.

## Active Anchors

- `{{PRIMARY_ANCHOR_1}}`: `{{PRIMARY_EXPLAINER_1}}`
- `{{PRIMARY_ANCHOR_2}}`: `{{PRIMARY_EXPLAINER_2}}`
- `{{PRIMARY_ANCHOR_3}}`: `{{PRIMARY_EXPLAINER_3}}`

## Working Rules

- Keep answers and edits aligned with the selected anchors.
- Mention the anchor when it materially affects the recommendation.
- Ask which anchor should dominate if two defaults point to different actions.
- Let direct user instructions override the defaults in this file.
