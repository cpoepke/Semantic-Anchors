# Semantic Anchors

Load these semantic anchors as always-on context for this workspace unless the user overrides them.

## Active Anchors

- `{{PRIMARY_ANCHOR_1}}`: `{{PRIMARY_EXPLAINER_1}}`
- `{{PRIMARY_ANCHOR_2}}`: `{{PRIMARY_EXPLAINER_2}}`
- `{{PRIMARY_ANCHOR_3}}`: `{{PRIMARY_EXPLAINER_3}}`

## Working Rules

- Apply the selected anchors to planning, code changes, reviews, and explanations.
- State which anchor is driving the recommendation when it is not obvious.
- Ask for precedence if multiple selected anchors conflict.
- Treat explicit user instructions as higher priority than this file.
